# SEC EDGAR requires a real contact email in the User-Agent header on every
# request, or it will return HTTP 403. Set this before running the server.
SEC_CONTACT_EMAIL=your-name@example.com

# Port the Express API listens on.
PORT=8787
